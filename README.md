## Requirements

- Go (version 1.23 or higher)
- Echo framework
- Logrus for logging
- Questions, options, correct answer can be changed from questions.go `getQuestions` function

## Usage
To start server:
./goquiz

then goto: http://localhost:5547

Select options and submit to see result